# STM32 下位机控制改进记录

> 本文件对应 2026-07-22 的当前代码，是本次升级后的生效说明。当前采用 5 ms/200 Hz 控制周期，并以 2 点滑动窗口统计最近 10 ms 的编码器脉冲。

## 1. 本次改进范围

目标工程：`ros-robot/ros-robot`，主控仍为 **STM32F103RB**，底盘仍为 **三轮全向轮**，保留原有蓝牙、USART/ROS 和 OLED 功能。

- 已迁移参考项目中较成熟的电机测速、速度控制、软启动和换向保护思路。
- 已接入 MPU6050 原始数据采集与上电零偏标定。
- **没有添加 CAN 通信。** 工程中原来随标准外设库存在的 CAN 驱动文件也没有被业务代码调用。
- 没有照搬参考项目的四轮差速运动学，三轮全向轮的正/逆运动学保持独立实现。

## 2. 已实现的改进点

| 模块 | 改进前 | 改进后 |
|---|---|---|
| 电机控制周期 | 5 ms | 5 ms（200 Hz FreeRTOS 周期任务）|
| 编码器读取 | 每周期读取后清零 | 16 位计数器自由运行，相邻采样作有符号差分，支持自然回绕 |
| 测速滤波 | 单次 5 ms 计数 | 5 ms 采样、2 点滑动窗口，即用最近 10 ms 的脉冲总数测速 |
| 电机/车轮参数 | 13 线、74.8:1、58 mm | 按参考项目预置为 11 线、4 倍频、30:1、65 mm |
| PWM | 约 1.95 kHz，初始化结构不完整 | TIM1 10 kHz，逻辑量统一为 0～1000，初始化字段完整 |
| PWM 保护 | 只限幅到 100% | 最大 60%（600/1000），非零最小有效值 5%（50/1000）|
| PID 时间尺度 | 积分、微分未使用 dt；微分符号相反 | 显式 `dt=0.005 s`，积分为 `e*dt`，微分为 `(e-e_last)/dt` |
| 积分保护 | 只有简单积分限幅 | 积分分离、积分限幅、输出饱和时撤销本周期积分 |
| 零点稳定性 | 无误差死区 | 误差绝对值不大于 3 mm/s 时 PID 校正为 0，并清除积分 |
| 速度前馈 | 无 | 每个轮子独立使用 `PWM_ff = 1.24 * wheel_target_mmps` |
| 目标速度 | 指令一步跳变 | 加速 500 mm/s²、减速 1000 mm/s² 的斜坡 |
| 正反转换向 | 可直接反打 H 桥 | 先减速到 0，保持 20 ms，再向相反方向加速 |
| 指令越界 | 三个轮速可能分别饱和、破坏运动方向 | 最大轮速超过 500 mm/s 时三个轮速同比例缩放 |
| 通信失联 | 已有 300 ms 保护 | 保留；超时或急停时绕过斜坡，立即 PWM 清零并复位控制器 |
| MPU6050 | 驱动存在但未启用，引脚与调试口/注释不一致 | PC0/PC1 软件 I²C，开漏，250 Hz，14 字节同步突发读取，上电陀螺零偏标定 |
| 芯片工程配置 | F103RB 却使用高容量启动文件 | 改为 `STM32F10X_MD` 和 `startup_stm32f10x_md.s`，与 128 KB F103RB 匹配 |

## 3. 为什么是“5 ms 控制 + 2 点窗口”

这不是把控制速度和测速稳定性二选一，而是把两者拆开：

1. 控制任务每 5 ms 醒来一次，目标斜坡、换向状态机、PID 和 PWM 均以 200 Hz 更新。
2. 每次取得最近 5 ms 的编码器增量，并放入长度为 2 的环形数组。
3. 新增一个增量的同时减去最旧增量，维护最近 10 ms 的脉冲总和；并不是每次重新遍历两个样本。
4. 速度由最近 10 ms 总脉冲计算，保留原10 ms测速分辨率，同时每5 ms更新一次结果。

参考电机参数下：

```text
每圈计数 = 11 × 4 × 30 = 1320 count/rev
轮周长   = π × 65 ≈ 204.20 mm
每个计数 = 204.20 / 1320 ≈ 0.15470 mm
10 ms 窗口速度 = 窗口总计数 × 0.15470 / 0.010
               ≈ 窗口总计数 × 15.47 mm/s
```

## 4. 三轮全向轮运动学和前馈初值

底盘中心到轮子的距离继续采用原工程的 `L = 117 mm`：

```text
wheel1 = 0.5 Vy + 0.8660254 Vx + 117 W
wheel2 = 0.5 Vy - 0.8660254 Vx + 117 W
wheel3 =       -1.0 Vy          + 117 W
```

其中 `Vx/Vy` 为 mm/s，`W` 为 rad/s，输出轮速也是 mm/s。先算三个轮速，再分别乘前馈系数 1.24；因此没有把差速四轮的左右轮 PWM 关系错误地搬过来。

几个初始计算示例：

| 底盘指令 | 三轮目标速度（mm/s） | 仅前馈得到的 PWM 初值 |
|---|---:|---:|
| `Vx=100, Vy=0, W=0` | `+86.60, -86.60, 0` | `+107, -107, 0` |
| `Vx=0, Vy=100, W=0` | `+50, +50, -100` | `+62, +62, -124` |
| `Vx=0, Vy=0, W=1` | `+117, +117, +117` | `+145, +145, +145` |

最终输出为：

```text
PWM = 速度前馈 + PID(斜坡目标轮速 - 滑窗实测轮速)
```

三个轮子目前均以 1.24 作为初值，因为准备使用相同型号的电机、减速箱和车轮。实车完成后应分别辨识三个轮子的前馈系数，以补偿电机差异、轮压和装配摩擦，不能把 1.24 当成永远不需要调整的常数。

## 5. PID 与保护参数

当前初值与参考工程一致：

```text
Kp = 4.0
Ki = 5.0
Kd = 0.01
dt = 0.005 s
误差死区 = 3 mm/s
积分分离阈值 = 50 mm/s
积分状态限幅 = ±50
PID 校正输出限幅 = ±1000
最终硬件 PWM 限幅 = ±600
```

控制规则：

- `|error| <= 3`：认为进入误差死区，PID 校正置零并清积分，减少静止抖动。
- `3 < |error| <= 50`：允许积分，以消除负载造成的小稳态误差。
- `|error| > 50`：积分清零，只由前馈、比例和微分快速处理大误差，避免启动阶段积分堆积。
- PID 输出达到上限时，撤销本周期新增积分，防止饱和后仍继续累积。
- 微分项使用正确的误差变化方向，并除以真实 `dt`。

这些参数是**可运行参考项目的初值**，不是未经实车验证就能保证最优的最终值。轮径、重量、电池电压、驱动器、电机和编码器装配变化后仍需重新整定。

## 6. 目标斜坡与换向保护

- 加速斜率：500 mm/s²，在 5 ms 内最多增加 2.5 mm/s。
- 减速斜率：1000 mm/s²，在 5 ms 内最多降低 5.0 mm/s。
- 普通指令从正转切到反转时，每个轮子独立执行：减速到 0 → PWM=0 保持 20 ms → 反方向按加速斜坡启动。
- 三个轮子分别维护状态，所以某一个轮子换向不会强制另外两个轮子一起停机。
- 蓝牙急停和 ROS 300 ms 超时属于安全事件，直接清 PWM、目标、斜坡和 PID，不等待软减速。

这样可降低 H 桥直通风险、电源冲击、电机齿轮反向冲击以及由于目标突变导致的 PID 微分冲击。

## 7. MPU6050 接线与数据

### 接线

```text
MPU6050 VCC -> 3.3 V
MPU6050 GND -> GND
MPU6050 SCL -> PC0
MPU6050 SDA -> PC1
MPU6050 AD0 -> GND（地址 0x68）
```

PC0/PC1 在当前工程中没有与电机方向、TIM1 PWM、三个编码器、OLED、USART2 或 USART3 冲突。软件 I²C 已改为开漏；常见 MPU6050 模块自带上拉，裸传感器或无上拉模块需要给 SCL/SDA 各加约 4.7 kΩ 到 3.3 V。

### 配置

- 加速度计：±4 g，换算系数约 8192 LSB/g。
- 陀螺仪：±500 °/s，换算系数约 65.5 LSB/(°/s)。
- 采样任务：4 ms，即 250 Hz。
- 每次从 `ACCEL_XOUT_H` 连续读 14 字节，确保加速度、温度和陀螺数据属于同一批寄存器快照。
- 开机完成 WHO_AM_I 检查后，静止采集 200 次，约 0.8 s，计算陀螺零偏；标定期间不要移动底盘。

应用层可直接读取：

```text
g_imuReady
g_imuAccelX / Y / Z
g_imuGyroX / Y / Z       （已减上电零偏）
g_imuTemperatureCentiDeg （摄氏度 × 100）
g_imuSampleCount
g_imuReadErrorCount
```

当前只完成可靠原始数据采集，没有启用 DMP，也没有把 IMU 强行并入里程计或姿态闭环。后续应先确认安装坐标系，再做坐标变换、互补滤波/EKF 和 ROS IMU 消息，避免“有数据但轴向错误”。

## 8. FreeRTOS 任务安排

| 任务 | 周期/触发 | 优先级 | 作用 |
|---|---:|---:|---|
| `control` | 5 ms | 5 | 编码器采样、滑窗测速、运动学、斜坡、前馈、PID、PWM |
| `ros_rx` | 队列事件 | 4 | USART3 DMA/IDLE 字节流解析 |
| `safety` | 20 ms | 4 | ROS 指令超时监控 |
| `imu` | 4 ms | 3 | MPU6050 14 字节突发读取 |
| `ros_tx` | 20 ms | 2 | 50 Hz 反馈发送 |
| `display` | 100 ms | 1 | OLED 显示 |

电机任务优先级最高；软件 I²C 任务可以被 5 ms 控制任务抢占，因此 IMU 读取不会长期阻塞电机控制。

## 9. 主要修改文件

- `Hardware/encoder.c/.h`：自由运行差分计数、2 点滑动窗口、参考电机参数。
- `Hardware/pid.c/.h`：显式 dt、积分分离、死区、积分/输出限幅、抗饱和。
- `Hardware/pwm.c/.h`：10 kHz PWM、0～1000 归一化占空比、完整初始化。
- `Hardware/motor.c/.h`：三轮运动学常数化、同比例限速、60%/5% PWM 保护、零输出安全状态。
- `User/app_tasks.c/.h`：200 Hz 控制任务、三轮前馈、目标斜坡、独立换向状态机、250 Hz IMU 任务。
- `Hardware/Mpu6050/MPU6050_I2C.c/.h`：PC0/PC1 开漏软件 I²C、ACK 错误检查、总线恢复；微秒延时使用 DWT 周期计数器，不改写 FreeRTOS SysTick。
- `Hardware/Mpu6050/mpu6050.c/.h`：250 Hz/±4 g/±500 °/s 配置、WHO_AM_I、14 字节突发读取。
- `User/main.c`：参考 PID 初值和 MPU 上电准备。
- `STM32 工程模版.uvprojx`：F103RB 中容量器件宏与启动文件修正。

## 10. 换电机后的首次上电检查

1. 先架空车轮或拆掉动力传动，禁止直接放地高速试车。
2. 确认安装的确是 11 线编码器、4 倍频、30:1 减速箱和 65 mm 车轮；未更换前不要用当前参数评价速度准确性。
3. 分别给三个轮子小正指令，检查“正 PWM → 正编码器速度”。如符号相反，应统一交换电机线或编码器相线/软件方向，不要靠把 PID 增益改成负数解决。
4. 检查驱动器能接受 10 kHz PWM，方向引脚电平逻辑与当前代码一致，并观察电源电流和驱动器温升。
5. 先只验证前馈，再逐步加入 PID；分别记录三个轮子的启动 PWM、100/200/300/400 mm/s 稳态 PWM，拟合各自前馈斜率和静摩擦偏置。
6. 再测试加减速、单轮换向、整车平移、旋转、复合运动和 ROS 断联急停。
7. 用逻辑分析仪或 GPIO 翻转测量 5 ms 控制任务执行时间和最坏抖动；若执行时间或抖动接近5 ms，再优化软件 I²C 或降低非关键任务负载。

## 11. 编译验证

已使用工程原有工具链 **Keil MDK 5.33 / ARMCC 5.06u7** 全量编译：

```text
startup_stm32f10x_md.s
0 Error(s), 0 Warning(s)
Code     = 18730 bytes
RO-data  = 1790 bytes
RW-data  = 356 bytes
ZI-data  = 14724 bytes
```

F103RB 的 20 KB SRAM 中，静态/链接 RAM 占用约为 `RW + ZI = 15128 bytes`，尚余约 5352 bytes。以上是编译和静态检查结果；电机方向、编码器极性、前馈/PID 最终值及 MPU 安装轴向仍必须在实物上验证。
